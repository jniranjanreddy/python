"""
What is generator?
It generates a sequence of values
It uses a special kkeyword yield

We can write generator functions just like other functions,
but it uses special keywork 'yield' to return values.

"""

